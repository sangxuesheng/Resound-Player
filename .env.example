# Web 开发模式使用的 API 地址（浏览器环境下）
# Electron 模式会优先使用 main/preload 注入的动态端口
VITE_API_BASE_URL=http://127.0.0.1:38761

# 可选：网易云 API 上游请求代理。
# 当前 NAS/容器环境如果无法直接解析或访问 music.163.com，可以填入可用 HTTP 代理。
# 示例：VITE_NCM_PROXY=http://127.0.0.1:7890
VITE_NCM_PROXY=
